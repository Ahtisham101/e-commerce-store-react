import { Box, Typography } from "@mui/material";
import React from "react";

export default function shirts() {
  return (
    <Box sx={{ mx: "auto" }}>
      <Typography sx={{ fontSize: "2rem" }}>shirts</Typography>
    </Box>
  );
}
