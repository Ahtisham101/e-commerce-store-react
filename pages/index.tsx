import type { NextPage } from "next";
import { Checkout } from "../src/Checkout";

import { HomePage } from "../src/HomePage";

const Home = () => {
  return <Checkout />;
  // <HomePage />;
};

export default Home;
